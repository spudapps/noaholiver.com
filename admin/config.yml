backend:
  name: git-gateway
  branch: main
  identity_url: "https://tubular-jelly-6f9063.netlify.app/.netlify/identity"
  gateway_url: "https://tubular-jelly-6f9063.netlify.app/.netlify/git"

media_folder: "assets/images"
public_folder: "/assets/images"

collections:
  - name: "posts"
    label: "Blog Posts"
    folder: "_posts"
    create: true
    slug: "{{year}}-{{month}}-{{day}}-{{slug}}"
    fields:
      - {label: "Title", name: "title", widget: "string"}
      - {label: "Date", name: "date", widget: "datetime"}
      - {label: "Body", name: "body", widget: "markdown"}
  
  - name: "settings"
    label: "Site Settings"
    files:
      - label: "Configuration"
        name: "config"
        file: "_config.yml"
        fields:
          - {label: "Site Title", name: "title", widget: "string"}
          - {label: "Description", name: "description", widget: "text"}
          - {label: "Theme", name: "remote_theme", widget: "string", hint: "e.g., pages-themes/hacker@v0.2.0"}
